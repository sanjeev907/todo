server {
    listen 80;
    server_name sanjeevdevops.xyz, www.sanjeevdevops.xyz;

    location /static/ {
        alias /app/static/;
    }

    location /media/ {
        alias /app/media/;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/uwsgi.sock;
    }
}
